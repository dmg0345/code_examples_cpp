/**
 ***********************************************************************************************************************
 * @file        exercise_38.cpp
 * @author      ${PROJECT_AUTHOR} (${PROJECT_CONTACT})
 * @date        ${PROJECT_BUILD_TIMESTAMP_UTC}
 * @version     ${PROJECT_VERSION}
 * @copyright   ${PROJECT_COPYRIGHT}
 ***********************************************************************************************************************
 */

namespace CppPrimer::Chapter2
{

void exercise_38(void)
{
    // You need to explicitly declare references with the auto type qualifier,
    // something that you don't have to do with decltype.
    // int i = 0;
    // int & ri = i;

    // A will be an int, aa will be a reference.
    // auto a = ri;
    // decltype(ri) aa = i;

    // b will be an int, bb will be a reference to int.
    // auto & b = ri;
    // decltype(ri) bb = i;
}

}

/******************************************************************************************************END OF FILE*****/
